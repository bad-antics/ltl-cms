// examples/demo.ltl — runnable demo for ltl_cms

import std::io
use ltl_cms as lib

fn main() {
    io::println("ltl_cms demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}